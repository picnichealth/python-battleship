# battleship_engine
A minimal battleship game engine.

Requirements:
  - python 3.6+
  - numpy
  - a terminal with unicode support

Run with:
```
  pip3 install -U numpy
  python3 -m battleship.game
```

You can also typecheck:
```
  pip3 install -U mypy
  mypy battleship
```
or run tests:
```
  pip3 install -U pytest
  pytest .
```

The game should look something like:

![Example Game](https://raw.githubusercontent.com/benpastel/battleship_engine/master/example_game.jpg?token=AAIN6GTAA76XQVAVLDGQCXC7J6MFE)

The interface assumes that `🚢💦💥` are exactly twice the width of ascii in your terminal output; otherwise it might look lopsided.

Happy hacking!

## AI engine for battleship
Requirements for ai_agent
- pytorch

Run with:
```
  pip3 install -U numpy
  pip3 install -U pytorch
  python3 -m ai_agent.game
```

Apply a deep reinforcement learning method based on REINFORCE algorithm (Policy Gradient.)

- Policy() and Policy2d() are two deep learning algorithm which is used to calculate 
the possibility of drop a bomb at each location. Policy() is based on DNN and Policy2d()
is based on CNN. In practice, the Policy() is more stable as I don't run the algorithm in 
too many epoch. 

- masked_softmax() is used to filtering out the invalid location, i.e., where has already been shot.

- get_reward() is used to generate score of each shot. +5 if hit ship -1 otherwise. You can try 
different reward value for this.

- choose_move() select a valid action based on the probability generated by Policy()

- In play_one_game(), I have add the Policy Gradient methods to train the model.

### TO do list

- modify reward function not only based on each shot but also based on if AI win.
- use other RL methods such as Q learning.
